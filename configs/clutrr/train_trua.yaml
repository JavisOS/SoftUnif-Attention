# CLUTRR TRUA training configuration

model_type: deberta
epochs: 10
lr: 0.00002
batch_size: 16
eval_batch_size: 32
num_workers: 0

root: data
dataset: data_089907f8

# Quote GPU ids to avoid YAML number coercion
gpus: "0"
strategy: single

seed: 42
lambda_nexthop: 1.0
lambda_edge: 1.0
lambda_consistency: 5.0
